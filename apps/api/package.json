{
  "name": "farsiai-api",
  "version": "0.4.8",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate-types": "wrangler types worker-configuration.d.ts",
    "check": "npm run generate-types && tsc -p tsconfig.json --noEmit",
    "test": "node --import ../../scripts/register-ts-loader.mjs --test test/index.test.ts test/desktop-regressions.test.ts test/v046-image-attachments.test.ts test/codex-pro.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.36.0"
  }
}
