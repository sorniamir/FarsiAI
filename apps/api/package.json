{
  "name": "farsiai-api",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --import ../../scripts/register-ts-loader.mjs --test test/index.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.36.0"
  }
}
