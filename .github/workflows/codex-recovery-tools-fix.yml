name: Fix Codex recovery tools

on:
  push:
    branches: ["agent/codex-pro-v0.4.7"]
    paths:
      - "scripts/fix-codex-recovery-tools.mjs"
      - ".github/workflows/codex-recovery-tools-fix.yml"

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/codex-pro-v0.4.7
          fetch-depth: 0
      - run: node scripts/fix-codex-recovery-tools.mjs
      - run: git diff --check
      - shell: bash
        run: |
          git config user.name "FarsiAI Codex CI"
          git config user.email "actions@users.noreply.github.com"
          git add apps/api/src/ai/agent-v2.ts
          git commit -m "Enable Codex diagnostic tools after failed writes"
          git push origin HEAD:agent/codex-pro-v0.4.7
